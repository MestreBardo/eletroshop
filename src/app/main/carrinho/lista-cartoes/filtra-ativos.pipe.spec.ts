import { FiltraAtivosPipe } from './filtra-ativos.pipe';

describe('FiltraAtivosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraAtivosPipe();
    expect(pipe).toBeTruthy();
  });
});
