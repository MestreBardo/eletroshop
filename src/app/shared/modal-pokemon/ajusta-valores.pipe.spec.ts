import { AjustaValoresPipe } from './ajusta-valores.pipe';

describe('AjustaValoresPipe', () => {
  it('create an instance', () => {
    const pipe = new AjustaValoresPipe();
    expect(pipe).toBeTruthy();
  });
});
